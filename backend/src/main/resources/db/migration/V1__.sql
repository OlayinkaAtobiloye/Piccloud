CREATE TABLE IF NOT EXISTS images (
                         id varchar(255),
                         file_name varchar(255),
                         date_created date,
                         image varbit
);